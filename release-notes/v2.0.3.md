# 贞贞的无限画布 v2.0.3

- 自动更新安装改为前台安装向导：下载完成后点击按钮会打开 NSIS 安装界面，不再让应用静默消失。
- 禁用 `autoInstallOnAppQuit`，下载完成后不会在用户退出应用时自动安装。
- 更新按钮文案改为“打开安装向导”，并补充安装状态提示。
- 新增 NSIS `installer.nsh`，尽量让新版安装器即使收到旧客户端的 `/S` 参数也切回前台安装。
- 聚合解析节点默认改为保存到本地输出目录，远端地址解析保留为高级模式；结果文案提示平台 CDN 链接可能因防盗链或时效签名直接打开 403。
