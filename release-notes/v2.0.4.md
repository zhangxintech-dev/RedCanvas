# 贞贞的无限画布 v2.0.4

- 同步 `wiltodelta/remove-ai-watermarks` 0.8.9，去AI水印节点支持新版 `controlnet`、`auto`、`adaptive-polish`、`min-resolution`、`controlnet-scale`、`unsharp` 与 GFPGAN 脸部恢复参数。
- 后端按实际运行时版本做 CLI 参数兼容：0.8.9+ 使用新版参数，旧 0.8.7 runtime 继续兼容 `ctrlregen` 与旧保护参数。
- 聚合解析节点默认保存到本地输出目录，远端地址解析继续作为高级模式保留，减少平台临时 CDN 链接直接打开 403 的困惑。
- 自动更新安装继续以前台 NSIS 安装向导显示，避免用户点击安装后应用消失却看不到安装过程。
- 打包规范继续要求公开 release 从 clean public worktree 构建，排除本地私有计费、支付、充值 overlay。
